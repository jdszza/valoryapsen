# Dependências de desenvolvimento/teste (não vão para as imagens Docker).
#   pip install -r tests/requirements-dev.txt
#   pytest tests/
pytest==9.1.1
pyflakes==3.4.0

# Os simuladores importam fastapi/pydantic no topo do módulo; o conftest só
# substitui `requests`, então estas duas precisam existir de verdade no venv.
fastapi==0.111.0
pydantic==2.7.1

# test_central_eventos.py importa central-computer/main.py de verdade. O banco
# é mockado (nenhum MySQL sobe), mas a cadeia de imports do módulo passa por
# orchestrator.py → httpx, database.py → pymysql e auth.py → jose/bcrypt.
# Versões espelhadas de central-computer/requirements.txt.
httpx==0.27.0
pymysql==1.1.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# test_ihm_relatorio.py importa ihm_web/app.py de verdade — o layout e o
# registro dos callbacks acontecem no import. Só `requests` é duplado.
# Versões espelhadas de ihm_web/requirements.txt.
dash==2.17.0
dash-bootstrap-components==1.6.0

# test_compose.py lê o docker-compose.yml (healthchecks, ciclos, portas).
PyYAML==6.0.1
