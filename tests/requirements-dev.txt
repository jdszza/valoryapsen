# Dependências de desenvolvimento/teste (não vão para as imagens Docker).
#   pip install -r tests/requirements-dev.txt
#   pytest tests/
pytest==9.1.1
pyflakes==3.4.0

# O `ruff` não é rodado pela suíte — ele é a varredura que se passa à mão antes
# de commitar. Fica aqui porque os achados dele já viraram correção uma vez
# (`zip()` sem `strict=`, `global` sem atribuição, closure que não liga a
# variável do laço), e sem a versão fixada a próxima passada acusa um conjunto
# diferente por causa da versão instalada, não do código.
#   python -m ruff check --select B905,B007,PLW2901,F841,F401,F541,PLW0602,B023 .
ruff==0.16.8

# Os simuladores importam fastapi/pydantic no topo do módulo; o conftest só
# substitui `requests`, então estas duas precisam existir de verdade no venv.
fastapi==0.111.0
pydantic==2.7.1

# test_central_eventos.py importa central-computer/main.py de verdade. O banco
# é mockado (nenhum MySQL sobe), mas a cadeia de imports do módulo passa por
# orchestrator.py → httpx, database.py → pymysql e auth.py → jose/bcrypt.
# Versões espelhadas de central-computer/requirements.txt.
httpx==0.27.0
pymysql==1.1.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# test_manut_relatorio.py importa manut_web/app.py de verdade — o layout e o
# registro dos callbacks acontecem no import. Só `requests` é duplado.
# Versões espelhadas de manut_web/requirements.txt.
dash==2.17.0
dash-bootstrap-components==1.6.0

# test_compose.py lê o docker-compose.yml (healthchecks, ciclos, portas).
PyYAML==6.0.1

# test_painel_ordens.py importa painel_operador/backend/app.py de verdade — as
# rotas são exercitadas pelo `app.test_client()` do Flask, sem subir servidor.
# O Flask fica um degrau abaixo do `requirements.txt` do painel (3.1.1) porque o
# `dash` do app de manutenção ainda exige `<3.1`, e os dois compartilham venv.
flask==3.0.3
qrcode==8.2

# test_serial_link.py exercita o transporte serial dos adapters de ponta a
# ponta, e para isso precisa do `serial_for_url` de verdade — é ele o ponto que
# faz o código não saber em qual sistema operacional está.
#
# A regra "nenhum teste abre porta física" continua valendo, e agora ela é mais
# do que promessa: as placas falsas de `tests/fakes/` falam por `socket://`, um
# handler de URL do PRÓPRIO pyserial, servido por um socket TCP em localhost.
# Nenhuma COM, nenhum /dev/tty. O painel de bancada segue com o duplo
# (`SerialFake` no conftest): lá o caminho que tocaria a porta é uma thread que
# nenhum teste sobe, e dublar continua sendo mais barato que encenar.
pyserial==3.5
